<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MediaBrowserPlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:PlayerFramework="using:Microsoft.PlayerFramework"
    xmlns:Classes="using:MediaBrowserPlayer.Classes"
    x:Class="MediaBrowserPlayer.PlayerPage"
    mc:Ignorable="d">

    <Page.Resources>
        <Classes:SecondsToTimeSpan x:Key="SecondsToTimeSpan"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button Content="Back" HorizontalAlignment="Left" Margin="21,365,0,0" VerticalAlignment="Top" Click="Button_Click" Width="150"/>
        <TextBox x:Name="stats" HorizontalAlignment="Left" Margin="24,256,0,0" TextWrapping="Wrap" VerticalAlignment="Top" RenderTransformOrigin="-3.211,-5.938" Width="144"/>
        <TextBox x:Name="progress" HorizontalAlignment="Left" Margin="24,293,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="144"/>
        <Slider x:Name="playbackProgress" HorizontalAlignment="Left" Margin="189,467,0,0" VerticalAlignment="Top" Width="1020" Height="39" ThumbToolTipValueConverter="{StaticResource SecondsToTimeSpan}"/>
        <PlayerFramework:MediaPlayer x:Name="mediaPlayer" Margin="189,57,157,311" Width="1020" Height="400" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <Button x:Name="pauseButton" Content="Pause" HorizontalAlignment="Left" Margin="21,422,0,0" VerticalAlignment="Top" Click="pauseButton_Click" Width="150"/>
        <TextBox x:Name="sessionInfo" HorizontalAlignment="Left" Margin="189,529,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="206" Width="1020"/>

    </Grid>
</Page>
